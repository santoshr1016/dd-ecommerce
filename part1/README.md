## Datadog Microservices

```

This work is replica of the Datadog microservice workshop however it has been modifed to a simpler version
There are 3 microservices 
Frontend Service 
Ads Service
Discount Service
Database(Postgress)


All the microservices are dockerized, Can be deployed using 

## Running the prject
POSTGRES_USER=postgres POSTGRES_PASSWORD=postgres docker-compose up --build

## Stopping the project
docker-compose down 

```

